<div class="top-bar">
        <div class="top-bar-left" >
            <ul class="menu">
                <li class="menu-text">
                    Message App
                </li>
            </ul>
        </div>
        <div class="top-bar-right">
          <ul class="dropdown menu" data-dropdown-menu>  
            <% if user_signed_in? %>
                 
              <a href="<%= messages_path %>">Posts</a>
                  <li>
                    <a><%= current_user.username %></a> 
                    <ul class="menu vertical">
                      <% if can? :manage, Admin %>
                        <li><a href="#">Admin</a></li>
                      <% end %>
                      <li><a href="<%= user_path(current_user)%>">User Profile</a></li>
                      <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete } %>
                    </ul> 
                  </li> 
              <% else %>  
                <%= link_to "Sign Up/Login", new_user_session_path %>
              <% end %>
            </ul>
        </div>
    </div>
 
    <script>
        $(document).foundation();
    </script>